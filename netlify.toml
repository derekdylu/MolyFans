# OpenClaw: frontend 在 frontend/ 子目錄
[build]
  base = "frontend"
  command = "npm run build"
  # 勿在 Netlify 後台把 Publish directory 設成 "frontend"，否則會只部署原始碼而 404

# 使用官方 Next.js plugin，才會用 OpenNext 產出正確的 SSR + 靜態資源
[[plugins]]
  package = "@netlify/plugin-nextjs"
